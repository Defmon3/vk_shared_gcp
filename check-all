#!/usr/bin/env bash
set -euo pipefail

uvx ruff check src tests
uvx black --check src tests
uv run mypy --config-file pyproject.toml src
uv run python -m pytest tests -c pyproject.toml -p no:cacheprovider -q
